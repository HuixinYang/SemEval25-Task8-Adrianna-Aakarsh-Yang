pip install gymnasium accelerate transformers databench-eval
pip install swig
pip install vllm
pip install auto-gptq # install auto-gptq package for handling GGUF format.
pip install auto-gptq --upgrade # Update the auto-gptq library for latest features.
pip install transformers --upgrade # Update the transformers library.
pip install optimum # Install the optimum library
pip install transformers>=4.30.0 # Update transformers to version 4.30.0 or later.
pip install --upgrade optimum auto-gptq transformers
pip install openai
pip install  bitsandbytes
pip install accelerate
pip install pandas
